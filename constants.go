package main

const (
	appName        = "EuroGoil"
	inputFilePath  = "config/config.yaml"
	outputFilePath = "output/output.yaml"

	queueMoreThanMax = 9999 // must be higher than 'max items' in any queue

	gas      = 0
	diesel   = 1
	LPG      = 2
	electric = 3
	register = 4
)
